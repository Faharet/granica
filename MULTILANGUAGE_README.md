# Многоязычность (Русский / Қазақша / English)

## Что было реализовано

В проект добавлена полная поддержка трех языков:
- **Русский (ru)** - язык по умолчанию
- **Қазақша (kk)** - казахский язык
- **English (en)** - английский язык

## Основные изменения

### 1. Настройки Django (settings.py)
- Добавлен `LocaleMiddleware` для обработки языков
- Настроены доступные языки в `LANGUAGES`
- Установлен русский язык как язык по умолчанию
- Добавлен путь к файлам переводов в `LOCALE_PATHS`

### 2. URL-маршруты (urls.py)
- Добавлен `i18n_patterns` для автоматического добавления префикса языка к URL
- Добавлен endpoint `/i18n/` для переключения языка
- Теперь все URL будут выглядеть так: `/ru/admin/`, `/kk/admin/`, `/en/admin/`

### 3. Переключатель языка
В верхнюю панель навигации добавлен выпадающий список для выбора языка:
- Отображается текущий язык
- Можно выбрать любой из трех доступных языков
- Выбор сохраняется в сессии пользователя

### 4. Файлы переводов
Созданы и заполнены файлы переводов:
- `locale/ru/LC_MESSAGES/django.po` - русские переводы
- `locale/kk/LC_MESSAGES/django.po` - казахские переводы
- Скомпилированы в `.mo` файлы для использования Django

## Как использовать

### Переключение языка
1. Откройте сайт
2. В правом верхнем углу найдите выпадающий список с языками (иконка 🌐)
3. Выберите нужный язык (Русский / Қазақша / English)
4. Страница автоматически перезагрузится с выбранным языком

### Доступ к разным языковым версиям напрямую
Вы можете напрямую открыть нужную языковую версию, добавив префикс к URL:
- Русский: `http://localhost:8000/ru/admin/`
- Казахский: `http://localhost:8000/kk/admin/`
- Английский: `http://localhost:8000/en/admin/`

## Для разработчиков

### Добавление новых переводов

1. **Найти строки для перевода:**
   В шаблонах Django используйте теги `{% trans %}` или `{% blocktrans %}`:
   ```django
   {% load i18n %}
   <h1>{% trans "Welcome" %}</h1>
   ```

   В Python коде используйте `gettext`:
   ```python
   from django.utils.translation import gettext as _
   message = _("Hello, world!")
   ```

2. **Обновить файлы переводов:**
   Если установлен GNU gettext:
   ```bash
   python manage.py makemessages -l ru
   python manage.py makemessages -l kk
   ```

   Или вручную отредактируйте файлы `.po` в `locale/ru/LC_MESSAGES/` и `locale/kk/LC_MESSAGES/`

3. **Скомпилировать переводы:**
   ```bash
   python compile_translations.py
   ```
   
   Или используйте Django команду (если установлен gettext):
   ```bash
   python manage.py compilemessages
   ```

### Добавление нового языка

1. Добавьте язык в `LANGUAGES` в `settings.py`:
   ```python
   LANGUAGES = [
       ('ru', 'Русский'),
       ('kk', 'Қазақша'),
       ('en', 'English'),
       ('uz', 'O\'zbekcha'),  # Новый язык
   ]
   ```

2. Создайте директорию для переводов:
   ```bash
   mkdir -p locale/uz/LC_MESSAGES
   ```

3. Создайте файл переводов:
   ```bash
   python manage.py makemessages -l uz
   ```

4. Заполните файл `locale/uz/LC_MESSAGES/django.po` переводами

5. Скомпилируйте:
   ```bash
   python compile_translations.py
   ```

## Файлы проекта

### Измененные файлы:
- `deli_bike_admin/settings.py` - добавлены настройки i18n
- `deli_bike_admin/urls.py` - добавлены i18n URL patterns
- `django_daisy/templates/admin/parts/navbar.html` - добавлен переключатель языка

### Новые файлы:
- `locale/ru/LC_MESSAGES/django.po` - русские переводы
- `locale/ru/LC_MESSAGES/django.mo` - скомпилированные русские переводы
- `locale/kk/LC_MESSAGES/django.po` - казахские переводы
- `locale/kk/LC_MESSAGES/django.mo` - скомпилированные казахские переводы
- `compile_translations.py` - скрипт для компиляции переводов

## Примечания

- Язык по умолчанию: **Русский**
- Язык сохраняется в сессии пользователя
- Переводы Django admin панели используют встроенные переводы Django
- Для полного перевода всех элементов нужно добавить больше строк в файлы `.po`

## Возможные проблемы и решения

**Проблема:** Переводы не отображаются после изменения .po файлов
**Решение:** Запустите `python compile_translations.py` для компиляции переводов

**Проблема:** При переключении языка URL не меняется
**Решение:** Убедитесь, что все ссылки в шаблонах используют тег `{% url %}` с правильными параметрами

**Проблема:** Некоторые элементы не переведены
**Решение:** Добавьте соответствующие строки в файлы `.po` и скомпилируйте их заново
